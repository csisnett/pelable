<h1>Edit Habit completion</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.habit_completion_path(@conn, :update, @habit_completion)) %>

<span><%= link "Back", to: Routes.habit_completion_path(@conn, :index) %></span>
