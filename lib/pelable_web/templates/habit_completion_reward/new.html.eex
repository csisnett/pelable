<h1>New Habit completion reward</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.habit_completion_reward_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.habit_completion_reward_path(@conn, :index) %></span>
