<h1>Edit Post</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.post_path(@conn, :update, @post.title || "Untitled", @post.uuid)) %>

<span><%= link "Back", to: Routes.post_path(@conn, :index) %></span>
